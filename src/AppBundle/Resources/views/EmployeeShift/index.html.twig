{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EmployeeShift list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Employeeid</th>
                <th>Shiftid</th>
                <th>Locationid</th>
                <th>Companyid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('manager_employees_shifts_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.employeeId }}</td>
                <td>{{ entity.shiftId }}</td>
                <td>{{ entity.locationId }}</td>
                <td>{{ entity.companyId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('manager_employees_shifts_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('manager_employees_shifts_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('manager_employees_shifts_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
